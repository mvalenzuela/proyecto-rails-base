<h2>Sign up</h2>

<%= devise_error_messages! %>

<div class="row">
  <div class="col-sm-6">
    <%= render 'people/form', person: @person, form_params: [resource, as: resource_name, url: registration_path(resource_name)], submit_btn_text: 'SignUp' %>
  </div>
</div>

<%= render "devise/shared/links" %>
